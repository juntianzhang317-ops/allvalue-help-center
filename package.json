{
  "name": "allvalue-help-center",
  "version": "1.0.0",
  "description": "AllValue 帮助中心 - 基于飞书知识库生成",
  "scripts": {
    "sync": "node sync.js",
    "build": "node build.js",
    "sync:build": "npm run sync && npm run build"
  },
  "dependencies": {
    "@feishu/node-sdk": "^1.0.0"
  }
}
